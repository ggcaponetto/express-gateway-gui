{
  "name": "express-gateway-gui",
  "private": false,
  "version": "0.0.3",
  "description": "GUI for express-gateway",
  "main": "index.js",
  "scripts": {
    "build": "lerna run --scope express-gateway-gui-gui build",
    "start": "lerna run --ignore express-gateway-gui-gui start",
    "start-dev": "lerna run --ignore express-gateway-gui-server start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Giuseppe Giulio Caponetto",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
